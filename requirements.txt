# --- Ingestion: Supabase → Snowflake ---
supabase>=2.15.0
snowflake-connector-python>=3.13.0
pandas>=2.2.0
python-dotenv>=1.0.0

# --- dbt + Snowflake adapter ---
# dbt-snowflake pulls in dbt-core automatically
dbt-snowflake>=1.9.0

# --- MetricFlow (semantic layer + mf CLI) ---
dbt-metricflow[snowflake]>=0.13.0
